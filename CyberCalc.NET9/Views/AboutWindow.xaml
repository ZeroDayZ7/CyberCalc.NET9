<Window x:Class="WPF_CALC_NET_9.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WPF_CALC_NET_9.Controls"
        xmlns:helpers="clr-namespace:WPF_CALC_NET_9.Helpers"
        helpers:DragBehavior.IsDraggable="True"
        Title="Settings"
        Height="250"
        Width="350"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True">
    <Border Background="{DynamicResource WindowBackground}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="5">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="35" />
                <!-- Nagłówek -->
                <RowDefinition Height="*" />
                <!-- Treść -->
                <RowDefinition Height="50" />
                <!-- Przycisk zamykania -->
            </Grid.RowDefinitions>
            <!-- Nagłówek -->
            <local:Header Grid.Row="0"
                          HeaderText="ABOUT" />
            <!-- Treść okna -->
            <StackPanel Grid.Row="1"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                <TextBlock Foreground="{DynamicResource TextColor}"
                           FontFamily="Consolas"
                           FontSize="14"
                           TextAlignment="Center"
                           Margin="0,5">
                    <Run Text="{StaticResource AppTitle}" />
                    <Run Text="{StaticResource AppVersion}" />
                </TextBlock>
                <TextBlock Text="Created by ZeroDayZ7"
                           Foreground="{DynamicResource TextColor}"
                           FontFamily="Consolas"
                           FontSize="14"
                           TextAlignment="Center"
                           Margin="0,5" />
                <TextBlock Text="© 2025"
                           Foreground="{DynamicResource TextColor}"
                           FontFamily="Consolas"
                           FontSize="14"
                           TextAlignment="Center"
                           Margin="0,5" />
            </StackPanel>
            <!-- Przycisk zamknięcia -->
            <local:CloseButton Grid.Row="2" />
        </Grid>
    </Border>
</Window>